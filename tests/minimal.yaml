# java -jar swagger-codegen-cli.jar generate -i tests/minimal.yaml -l html -o tests
openapi: 3.0.0

info:
  title: API
  description: Process.
  version: 1.0.0

tags:
  - name: info
    description: Provides info.

paths:

  /info:
    post:

